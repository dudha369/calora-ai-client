/**
 * Цвета точек-маркеров активности (DateStrip + Calendar): показывают,
 * есть ли за день записи еды и/или воды.
 *
 * FOOD — фирменный зелёный приложения (тот же, что у ростка рядом
 * с логотипом Calora AI на HomePage и в favicon.svg).
 * WATER — тот же синий, что уже используется во всём разделе воды
 * (см. WATER_BLUE в WaterPage.tsx) — маркер должен совпадать по цвету
 * с разделом, который он представляет, а не вводить третий оттенок синего.
 */
export const MARKER_FOOD_COLOR = '#90EE90';
export const MARKER_WATER_COLOR = '#4FC3F7';
